# workflow to mark checks pass when non path related changes

name: test

on:
  pull_request:
    paths-ignore:
      - melpoi/**
      - poetry.lock
      - pyproject.toml
      - .github/workflows/**
      - tests/**
      - .flake8

jobs:
  checks:
    uses: ./.github/workflows/dummy_test.yml
