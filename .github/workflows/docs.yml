# workflow to mark checks pass when non path related changes

name: docs

on:
  pull_request_target:
    paths-ignore:
      - melpoi/**
      - poetry.lock
      - pyproject.toml
      - .github/workflows/**
      - tests/**
      - .flake8

jobs:
  checks:
    uses: ./.github/workflows/dummy_test.yml
